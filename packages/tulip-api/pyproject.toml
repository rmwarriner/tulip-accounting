[project]
name = "tulip-api"
version = "0.0.0"
description = "FastAPI server for Tulip Accounting."
requires-python = ">=3.12"
dependencies = [
    "tulip-core",
    "tulip-storage",
    "tulip-importers",
    "fastapi>=0.115",
    "python-multipart>=0.0.9",
    "pydantic>=2.7",
    "pydantic[email]>=2.7",
    "email-validator>=2.2",
    "uvicorn>=0.30",
    "structlog>=24.1",
    "argon2-cffi>=23.1",
    "pyjwt>=2.9",
    "pyotp>=2.9",
    "qrcode>=7.4",
    "slowapi>=0.1.9",
    "httpx>=0.27",
]

[tool.uv.sources]
tulip-core = { workspace = true }
tulip-storage = { workspace = true }
tulip-importers = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tulip_api"]
