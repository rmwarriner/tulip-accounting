[project]
name = "tulip-cli"
version = "0.0.0"
description = "Command-line client (Typer) for the Tulip Accounting API."
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tulip_cli"]
